# Training-machine requirements (Quadro RTX 5000, standard x86_64 CUDA install).
#
# The Jetson AGX Orin deployment target does NOT use this file -- plain
# `pip install torch` does not work on Jetson (PyPI wheels are x86_64 only;
# Jetson is aarch64 with a JetPack-pinned CUDA/PyTorch build). See README
# "Running on the Jetson" for the correct install path there.

torch>=2.0
numpy>=1.24
pandas>=2.0
pytest>=7.0
opencv-python-headless>=4.8  # ros2_inference/run_inference_on_bag.py --out-video mp4 writing
